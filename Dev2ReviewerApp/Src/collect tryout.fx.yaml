"'collect tryout' As screen":

    Button2 As button:
        OnSelect: |-
            =ClearCollect(tryitem,checkpointItem_ActionItem);
            ClearCollect(tryitem1,checkpointItem_ActionItem);
            ClearCollect(tryitem2,checkpointItem_ActionItem);
            ClearCollect(tryitem3,checkpointItem_ActionItem);
            ClearCollect(tryitem4,checkpointItem_ActionItem);
            ClearCollect(tryitem5,checkpointItem_ActionItem);
            ClearCollect(tryitem6,checkpointItem_ActionItem);
            
            ClearCollect(tryitem7,tryitem,tryitem1,tryitem2,tryitem3,tryitem4,tryitem6,tryitem6);
        Text: ="Collect"
        X: =58
        Y: =130
        ZIndex: =1

    Label1 As label:
        Height: =45
        Text: =CountRows(tryitem)
        Width: =430
        X: =92
        Y: =212
        ZIndex: =2

    Label2 As label:
        Height: =42
        Text: =CountRows(tryitem7)
        Width: =332
        X: =92
        Y: =327
        ZIndex: =3

